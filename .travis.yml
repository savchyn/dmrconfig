sudo: false
os: linux
language: c
before_install:
  sudo apt-get install libusb-1.0-0-dev

deploy:
  provider: releases
  api_key: "GITHUB OAUTH TOKEN"
  file: dmrconfig
  skip_cleanup: true
  keep-history: true
  on:
    branch: bin
    tags: true
after_success:
  bash <(curl -s https://codecov.io/bash)
